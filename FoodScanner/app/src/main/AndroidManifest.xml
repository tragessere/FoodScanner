<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="senior_project.foodscanner" >

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MealCalendar"
            android:label="@string/title_activity_meal_calendar" >
        </activity>
        <activity
            android:name=".activities.MealDetails"
            android:label="@string/title_activity_meal_details" >
        </activity>
        <activity
            android:name=".activities.FoodScanner"
            android:label="@string/title_activity_food_scanner" >
        </activity>
    </application>

</manifest>
